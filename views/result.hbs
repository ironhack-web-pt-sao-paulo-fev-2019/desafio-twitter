<div class="box-result">
    <!-- Busca por Tweet e Hashtag -->
    {{#each data}}
    {{>tweets}}
    {{/each}}
    <!-- Busca por Username -->
    {{#each dataUser}}
    {{>users}}
    {{/each}}
</div>